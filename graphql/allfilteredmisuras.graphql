#import "./fragments.graphql"

query AllFilteredMisuras ($idMisure: [ItemId]) {
  allMisuras(
    filter: {
      id: {
        in: $idMisure
      }
    }
  ) {
    _createdAt
    _updatedAt
    label
    id
    pacchetto
    idSalesforce
  }
  _allMisurasMeta {
    count
  }
}

